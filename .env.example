# ===========================================
# ContextStream Environment Configuration
# ===========================================
# Copy this file to .env and fill in your actual values

# ===========================================
# Application Settings
# ===========================================
#NODE_ENV=development
APP_PORT=3000

# ===========================================
# Database Configuration
# ===========================================
# For Neon Postgres - Vercel uses POOLER by default:
# Pooler URL format: postgresql://user:pass@xxx-pooler.xxx.neon.tech/db?sslmode=require
# DATABASE_URL=postgresql://user:password@host-pooler.neon.tech/database?sslmode=require

# IMPORTANT: Only set NEON_ADD_CHANNEL_BINDING=true for DIRECT connections (not pooler)
# - Pooler connections (URL contains "pooler"): Do NOT use channel_binding
# - Direct connections (URL without "pooler"): Use channel_binding=require
# Most Vercel deployments use pooler, so leave this commented out
# NEON_ADD_CHANNEL_BINDING=false

# For local Postgres (Docker):
# POSTGRES_USER=contextstream
# POSTGRES_PASSWORD=changeme
# POSTGRES_DB=contextstream
# POSTGRES_PORT=5432
# DATABASE_URL=postgresql://contextstream:changeme@localhost:5432/contextstream

# ===========================================
# Redis Configuration
# ===========================================
REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password_here

# ===========================================
# Authentication (Better Auth)
# ===========================================
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-long-change-this-in-production
# Base domain for authentication (without protocol, without www)
# Development: localhost:3000 (will use http://)
# Production: contextstream.dev (code will automatically add www to match vercel.json redirect)
# NOTE: Client uses relative URLs, this is only for server-side OAuth callbacks
# IMPORTANT: Make sure your OAuth callback URLs use https://www.yourdomain.com in production
NEXT_PUBLIC_BETTER_AUTH_DOMAIN=localhost:3000

# ===========================================
# AI Services
# ===========================================
OPENAI_API_KEY=your_openai_api_key_here

# ===========================================
# OAuth Providers
# ===========================================
# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# GitHub OAuth
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# ===========================================
# Stripe (if using payments)
# ===========================================
# STRIPE_SECRET_KEY=your_stripe_secret_key_here
# STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here
# STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here

# ===========================================
# Email (if using email services)
# ===========================================
# EMAIL_FROM=noreply@yourdomain.com
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password

# ===========================================
# File Storage (if using cloud storage)
# ===========================================
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-s3-bucket-name

# ===========================================
# Monitoring & Analytics (optional)
# ===========================================
# SENTRY_DSN=your_sentry_dsn_here
# GOOGLE_ANALYTICS_ID=your_ga_id_here

# PDF Parser Service
PDF_PARSER_URL=http://localhost:8001
